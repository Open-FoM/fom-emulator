/** Item override hooks (runtime stats + optional base table). */
#pragma once

#include "HookDetours.h"

/** Installs item override hooks (CShell.dll) when enabled. */
void EnsureItemOverrideHooks();
