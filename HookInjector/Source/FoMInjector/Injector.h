/** Injector stub entrypoint declarations. */
#pragma once

#include <windows.h>

/** WinMain entrypoint for the injector stub. */
int WINAPI WinMain(HINSTANCE Instance, HINSTANCE PrevInstance, LPSTR CmdLine, int ShowCmd);
