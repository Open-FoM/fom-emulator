/**
 * FoM World Server
 *
 * Handles in-world gameplay after players connect from the Master server.
 */

import { RakPeer, getVersion } from '@openfom/networking';
import { info } from '@openfom/utils';

info('='.repeat(60));
info(' FoM World Server');
info('='.repeat(60));
info(`  RakNet Version: ${getVersion()}`);
info('');
info('World server implementation pending...');
